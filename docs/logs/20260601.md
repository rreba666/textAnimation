# 开发日志

## 项目：儿戏的日常手记

纯前端个人仪表盘，React 18 + TypeScript + Vite 5 + TailwindCSS 3 + Zustand 5 + Recharts + lucide-react。

---

## 2026-06-01（初始搭建）

### 最终交付（15 组件，34 源码文件）

#### 配置（10）
`package.json` / `vite.config.ts` / `tsconfig.json` / `tsconfig.node.json` / `tailwind.config.js` / `postcss.config.js` / `index.html` / `.env.example` / `.gitignore` / `.claude/CLAUDE.md`

#### 核心（4）
- `src/types/index.ts` — Todo(含completedAt) / Note / Habit / LinkItem / WeatherData / ForecastDay
- `src/utils/storage.ts` — localStorage 读写 + crypto.randomUUID ID 生成
- `src/store/useDashboardStore.ts` — Zustand v5 单 store + persist + 主题 + HMR 保护
- `src/vite-env.d.ts` — Vite 环境声明

#### 组件（15）
| 组件 | 功能 |
|------|------|
| Header | 问候 + 时钟 + 日期 + 天气 + 每日一言 + 主题切换 + 进度条 + 倒计时 |
| Greeting | 6 时段昼夜问候（Sunrise/Sun/Coffee/Moon 等 lucide 图标） |
| ProgressBar | 当月进度条（渐变填充） |
| DailyQuote | hitokoto.cn API + 5 条离线回退 + 手动换句 |
| Countdown | 今日剩余 HH:MM + 本周剩余 X 天 |
| Todo | CRUD + 进度统计 + 圆角方形复选框 + 滚定高度 |
| Habits | 预设 5 习惯 + 爱心/星星打卡 + Recharts 月统计 + 最高连续天数 |
| Links | Google Favicon + 名称 + 域名双行 + 响应式网格 |
| PomodoroTimer | SVG 圆环 + 25/15/5 预设 + Web Audio 蜂鸣 + Notification |
| WeekStats | 本周完成待办/打卡次数/连续天数/新增笔记 |
| Calendar | 月视图 7 列 + 打卡绿点 + 今日高亮 + 前后翻月 |
| Notes | 左列表 + 右 Markdown 编辑/预览 + JetBrains Mono 代码 + 1s 防抖保存 |
| EasterEgg | 右下角星星，10 句随机语录 |
| ErrorBoundary | 渲染异常捕获 + 刷新按钮 |

#### 构建记录
- TypeScript 严格模式：零错误
- Vite 生产构建：通过
- 部署包：`deploy-frontend-20260601.tar.gz`

#### 技术决策
| 决策 | 选择 | 原因 |
|------|------|------|
| 天气 | Open-Meteo | 免费，无需 API Key |
| 配色 | RGB 通道 CSS 变量 | TailwindCSS 3 只在此格式下保留 var() 引用 |
| 字体 | 钉钉进步体 + JetBrains Mono | 温暖手帐风格 + 清晰代码 |
| 状态 | Zustand v5 persist | 单 store + 自动 localStorage 同步 |
| 主题 | data-theme 属性 | 即时切换，CSS 变量驱动 |

---

## 2026-06-02（交互优化 + 功能扩展）

### 第一次迭代：GSAP 动画 + 键盘快捷键 + 删除撤销

**新增依赖**：`gsap` 3.15.0

| 文件 | 改动 |
|------|------|
| `src/components/Habits.tsx` | 打卡点击增加 GSAP 弹性缩放动画（`back.out(3)` 缓动，缩至 0.5 倍弹回） |
| `src/components/Todo.tsx` | 新增待办 GSAP 入场动画（`opacity: 0, x: -20` 滑入淡入）+ 快捷键 T/E 监听 |
| `src/components/Notes.tsx` | 快捷键 N 监听（无笔记时自动创建再聚焦）+ 标题输入框 GSAP 脉冲反馈 |
| `src/App.tsx` | 全局 `keydown` 监听，分发 `shortcut:focus-note` / `shortcut:focus-todo` / `shortcut:edit-todo` 自定义事件 |

快捷键：

| 按键 | 功能 | 备注 |
|------|------|------|
| N | 聚焦笔记标题 | 无选中笔记时自动新建 |
| T | 聚焦待办输入框 | 附带脉冲高亮反馈 |
| E | 编辑首个未完成待办 | 无待办时无操作 |

### 第二次迭代：Toast + 待办删除撤销

| 文件 | 改动 |
|------|------|
| `src/components/Toast.tsx` | 新建：手帐风格全局提示，右侧滑入、40px 胶囊圆角、SVG 星形装饰、撤销按钮、倒计时进度条、GSAP 入场/退场动画 |
| `src/components/Todo.tsx` | 删除动画（GSAP 收缩淡出）+ Toast 撤销提示 + 3 秒自动消失 |

删除撤销流程：
```
点击删除 → GSAP 收缩淡出(0.3s) → 移除 → Toast（含撤销按钮）
                                    ↓ 3秒内
                              撤销 → addTodo 恢复 + 入场动画
                              超时 → Toast 自动消失
```

### 第三次迭代：待办拖拽排序 + Toast 深色适配

| 文件 | 改动 |
|------|------|
| `src/store/useDashboardStore.ts` | 新增 `reorderTodos(fromId, toId)` 拖拽排序方法 |
| `src/components/Toast.tsx` | 背景改用 `rgb(var(--bg-card))` 适配双主题；新增底部 GSAP 倒计时进度条 |
| `src/components/Todo.tsx` | HTML5 拖拽排序：GripVertical 手柄、draggable、放置后 GSAP 落地脉冲 |

### 第四次迭代：加载骨架屏

| 文件 | 改动 |
|------|------|
| `src/components/Skeleton.tsx` | 新建：`Shimmer` 流光基础块 + 7 张卡片骨架 |
| `src/App.tsx` | 新增 `phase` 状态机（skeleton→content）、700ms 最短展示、GSAP 淡出/淡入过渡 |

骨架屏动画时序：
```
挂载 → DashboardSkeleton 淡入(0.2s) → Shimmer 流光循环(1.8s/次)
                    ↓ 700ms
         骨架屏 GSAP 淡出(0.3s) → 内容层 GSAP 淡入+上移(0.4s)
```

### 第五次迭代：周报弹窗 + 月度趋势弹窗

| 文件 | 改动 |
|------|------|
| `src/components/WeeklyReport.tsx` | 新建：模态弹窗，GSAP `back.out(1.6)` 弹性入场 + Recharts 折线图 + ESC 关闭 |
| `src/components/WeekStats.tsx` | 右上角新增 SVG 迷你图表按钮 |
| `src/components/MonthlyTrend.tsx` | 新建：月份切换 + 每日完成折线图 + 4 列统计摘要 + GSAP 入场/退场动画 |
| `src/components/Todo.tsx` | 标题栏右侧新增月度趋势入口按钮 |

弹窗交互：遮罩 `rgba(0,0,0,0.35)` 点击关闭 / ESC 关闭 / GSAP `back.out(1.6)` 入场 + `power2.in` 退场

### 第六次迭代：打卡热力图 + 数据导入导出

| 文件 | 改动 |
|------|------|
| `src/components/HeatmapModal.tsx` | 新建：GitHub 风格全年热力图 + 年份切换 + 数据导入导出 + GSAP 动画 |
| `src/components/Habits.tsx` | 标题栏右侧新增表格 SVG 图标按钮 |

热力图：7 行（周一~周日）× ~53 列，颜色深浅 = 当天打卡数量，热力图绿色系 `accent-green`

数据导出：`JSON.stringify` → `Blob` → `<a>` download
数据导入：`<input type="file">` → 解析 JSON → `showConfirm` 二次确认 → `setState` 覆盖

### 第七次迭代：Markdown 导出

| 文件 | 改动 |
|------|------|
| `src/components/WeeklyReport.tsx` | report 新增 habitDetails/weekNoteTitles/weekNum；底部新增"导出为 Markdown"按钮 |
| `src/components/Notes.tsx` | 编辑器工具栏新增 FileDown 导出按钮 |

---

## 2026-06-03（个性化 + 通知 + 拖拽升级 + PWA）

### 第八次迭代：卡片显隐 + 布局记忆 + 背景切换

| 文件 | 改动 |
|------|------|
| `src/store/useDashboardStore.ts` | 新增 `hiddenCards`/`cardOrder`/`background` 状态 + partialize |
| `src/components/CardMenu.tsx` | 新建：卡片「⋯」下拉菜单（隐藏卡片入口） |
| `src/components/ManageCardsModal.tsx` | 新建：7 张卡片列表 + 眼睛开关显隐 + 拖拽排序 |
| `src/components/BackgroundPicker.tsx` | 新建：4 种背景预设（米白纸纹/淡粉渐变/水墨灰/森林绿） |
| `src/App.tsx` | 卡片按 cardOrder 渲染 + hiddenCards 过滤 + 整卡拖拽排序 + 动态背景 |
| `src/components/Header.tsx` | 顶部栏新增管理卡片 + 背景切换按钮 |

背景策略：米白纸纹（默认）不设内联样式，CSS 变量随主题变色；其他预设覆盖固定颜色

### 第九次迭代：通知与提醒系统

| 文件 | 改动 |
|------|------|
| `src/types/index.ts` | Todo 新增 `reminderAt?: string` |
| `src/store/useDashboardStore.ts` | 新增 `setTodoReminder` + 番茄钟持久化字段 |
| `src/hooks/useTodoReminder.ts` | 新建：每 60s 检查到期提醒并弹通知 |
| `src/hooks/useHabitReminder.ts` | 新建：每小时检查，晚 8 点提醒未打卡习惯 |
| `src/components/PomodoroTimer.tsx` | 重写：状态持久化到 store，刷新自动恢复 |
| `src/components/Todo.tsx` | 新增铃铛图标 + 时间选择器设置提醒 |
| `src/App.tsx` | 通知权限请求 + 每日首次打开昨日总结 + hook 集成 |

通知功能清单：

| 功能 | 触发时机 | 实现 |
|------|---------|------|
| 权限请求 | 页面加载 2 秒后 | `Notification.requestPermission()` |
| 番茄钟结束 | 倒计时归零 | beep + notify |
| 待办提醒 | HH:mm 到期 | hook 每分钟检查 |
| 打卡提醒 | 每晚 20:00 | hook 每小时检查，localStorage 防重复 |
| 每日总结 | 每日首次打开 | 昨日完成待办/打卡/连续天数 |

### 第十次迭代：Bug 修复

| 问题 | 修复 |
|------|------|
| Notification 404 | 移除所有 `icon: '/favicon.svg'` 参数 |
| 番茄钟刷新重置 | 状态持久化到 store，刷新扣除已流逝时间 |
| 热力图标签错位/溢出 | 月份标签改为绝对定位 + flex margin 修复 |
| 热力图无颜色 | `rgba()` 逗号混用改为 CSS Color Level 4 `rgb(xxx / alpha)` 语法 |
| 弹窗 hover 缩放/闪烁 | Portal 渲染到 body 脱离卡片 DOM 树 |
| 背景不随主题变色 | 纸纹默认返回 undefined 不设内联样式 |

### 第十一次迭代：dnd-kit 拖拽排序

**新增依赖**：`@dnd-kit/core` 6.3.1 / `@dnd-kit/sortable` 10.0.0 / `@dnd-kit/utilities` 3.2.2

| 文件 | 改动 |
|------|------|
| `src/App.tsx` | 移除 HTML5 DnD，迁移到 dnd-kit：`DndContext` + `SortableContext` + `useSortable` + `DragOverlay` |

dnd-kit 架构：
```
DndContext (PointerSensor 8px阈值 + KeyboardSensor)
  └── SortableContext (rectSortingStrategy)
        ├── SortableCard ← useSortable({ id })
        └── ...
  └── DragOverlay ← 被拖拽卡片浮动副本
```

迁移对比：

| 项目 | HTML5 DnD | dnd-kit |
|------|-----------|---------|
| 碰撞检测 | 手动 ID 匹配 | `closestCenter` |
| 拖拽预览 | 浏览器幽灵图 | `DragOverlay` 组件副本 |
| 键盘支持 | 无 | `KeyboardSensor` + 方向键 |
| 位移动画 | 手动 CSS | `useSortable` transform + transition |
| 排序算法 | 手动 splice | `arrayMove` 工具函数 |

拖拽动画：DragOverlay 入场 `back.out(2)` 弹出 → 原位 `opacity:0.35` → 松手 GSAP `elastic.out` 回弹

### 第十二次迭代：PWA + 确认弹窗

| 文件 | 改动 |
|------|------|
| `public/manifest.json` | 新建：PWA 清单（应用名"儿戏的日常手记"/图标/主题色 `#D4A5A5`） |
| `public/sw.js` | 新建：Service Worker（App Shell 预缓存 + 缓存优先策略 + 离线回退） |
| `public/icon-192.svg` | 新建：192x192 PWA 图标（手帐笔记本 SVG） |
| `public/icon-512.svg` | 新建：512x512 PWA 图标 |
| `public/favicon.svg` | 更新为 PWA 统一图标风格 |
| `index.html` | 新增 manifest/SW 注册/`theme-color`/iOS meta/Edge 磁贴 |
| `src/components/Header.tsx` | 主题切换右侧新增 PWA 安装按钮（手绘 SVG） |
| `src/components/ConfirmDialog.tsx` | 新建：函数式调用 `showConfirm()`，手帐风格，GSAP 动画 |
| `src/components/HeatmapModal.tsx` | `window.confirm` 替换为 `showConfirm` |

PWA 安装按钮显示逻辑：

| 环境 | 行为 |
|------|------|
| 线上 HTTPS | `beforeinstallprompt` 触发 → 点击弹出浏览器原生安装弹窗 |
| localhost | 按钮常显 → 点击弹出 `showConfirm` 引导提示 |
| 已安装后 | `appinstalled` 事件 → 按钮隐藏 |

确认弹窗 API：
```ts
const ok = await showConfirm({ title: '标题', message: '内容' })
// cancelText: '' → 隐藏取消按钮（单按钮模式）
```

---

### 第十三次迭代：待办 dnd-kit 迁移 + 习惯卡片滚动修复

| 文件 | 改动 |
|------|------|
| `src/components/Todo.tsx` | HTML5 DnD 迁移到 dnd-kit：`SortableContext` + `useSortable` + `rectSortingStrategy`，撤销 DragOverlay（滚动容器内坐标系冲突），transform 仅保留 translate3d 防挤压 |
| `src/components/Habits.tsx` | 滚动区 `max-h-[320px]` 防止撑开卡片 |

### 待办 dnd-kit vs 卡片 dnd-kit 差异

| 特性 | 卡片 | 待办 |
|------|------|------|
| DragOverlay | 使用（浮层副本 + GSAP 入场缩放） | 不使用（滚动容器内坐标系冲突） |
| 视觉反馈 | `useSortable` opacity + DragOverlay | `useSortable` opacity 0.3 + transform 平移 |
| transform | `CSS.Transform.toString` 全量 | 仅 `translate3d(x, y, 0)` 忽略 scale |
| 策略 | `rectSortingStrategy` | `rectSortingStrategy` |
| 传感器 | PointerSensor(8px) + KeyboardSensor | PointerSensor(5px) |

### 为什么待办不用 DragOverlay

`DragOverlay` 通过 `position: fixed` 渲染到 viewport，但 `DndContext` 在滚动容器 (`overflow-y-auto`) 内部时，dnd-kit 计算的坐标基于滚动容器，导致浮层偏移到鼠标右下方。提升 `DndContext` 到卡片层级仍无法解决（`SortableContext` 的 rect 测量仍受滚动影响）。移除 `DragOverlay`，依赖 `useSortable` 自带的 transform 平移 + opacity 反馈，效果等效且无错位。

### 2026-06-03 第十四次迭代：卡片入场动画 + 数字滚动 + 番茄钟闪烁 + 欢迎弹窗

| 文件 | 改动 |
|------|------|
| `src/App.tsx` | 骨架屏消失后卡片依次淡入上浮：`gsap.fromTo(.card-item, { opacity:0, y:30 }, { stagger:0.05, back.out(1.2) })` |
| `src/components/AnimatedNumber.tsx` | 新建：`<AnimatedNumber value={n} />`，GSAP `fromTo({ val:prev }, { val:next }, { onUpdate })` 滚动到新值，0.8s power2.out |
| `src/components/Todo.tsx` | 统计数字改用 AnimatedNumber |
| `src/components/Habits.tsx` | 连续天数改用 AnimatedNumber |
| `src/components/WeekStats.tsx` | 4 项统计改用 AnimatedNumber |
| `src/components/PomodoroTimer.tsx` | 剩余 ≤10s 数字变红 `#e05555` + GSAP `scale 1→1.08` yoyo 脉冲 |
| `src/components/DataTipModal.tsx` | 重写：合并快捷键帮助 + 3 秒确认倒计时 + 导出按钮 |
| `src/components/Header.tsx` | 新增 HelpCircle 帮助按钮（重新打开欢迎弹窗） |

### 2026-06-03 第十五次迭代：待办划线动画（已移除）

添加了 CSS `::after` + `scaleX` 逐字划线动画，0.35s ease-out。后因效果不佳移除，恢复即时切换。

### 2026-06-03 第十六次迭代：卡片时间微调 + 成就徽章

| 文件 | 改动 |
|------|------|
| `src/App.tsx` | 新增 `data-time-period` 属性：根据小时设置 morning/noon/afternoon/night |
| `src/index.css` | 新增 4 个时段卡片底色微调规则（`linear-gradient` 叠加，透明度 2-3%） |
| `src/components/AchievementsModal.tsx` | 新建：4 个徽章（第七个早安/一百件小事/拾贝/笔尖漫步），各自手绘 SVG 图标，已获得/未获得分区，未获得带进度条，`checkAchievements()` 检测函数 |
| `src/components/Header.tsx` | Award 徽章入口按钮 + AchievementsModal 弹窗集成 |
| `src/App.tsx` | 首次加载 + 每 30s 定时调用 `checkAchievements()`，达成条件弹出 Toast |

### 卡片"呼吸感"

```
5:00-11:00  上午  → 卡片叠 3% 暖橘 tint，视觉微温
11:00-13:00 中午  → 2.5% 暖黄，中性
13:00-18:00 下午  → 3% 冷绿 tint，视觉微凉
18:00-5:00  夜间  → 2% 微灰，弱降饱和度
```

### 成就徽章

| 徽章 | 条件 | 意义 |
|------|------|------|
| 第七个早安 | 连续打卡 7 天 | 一周的晨光，你都接住了。 |
| 一百件小事 | 完成 100 个待办 | 大事轮不到我，但小事被我做完了。 |
| 拾贝 | 新增 10 个链接 | 小小的收集，不为什么，只是喜欢。 |
| 笔尖漫步 | 新增 20 篇笔记 | 字迹歪歪扭扭也没关系，你走过的路，笔都记得。 |

### 2026-06-03 第十七次迭代：卡片质感 + 图钉装饰 + 成就偷看 + 提醒修复

| 文件 | 改动 |
|------|------|
| `src/index.css` | 卡片阴影加深 50%，亮色下与背景对比更强 |
| `src/App.tsx` | SortableCard 新增图钉装饰（6 色彩色，18% 比例定位，hover 微抬微旋，拖拽变 SVG 虚线圆孔） |
| `src/components/AchievementsModal.tsx` | 「偷看一眼」滑动开关，仅控制寄语显隐，条件始终可见 |
| `src/components/Todo.tsx` | 提醒弹窗 createPortal + 透明遮罩失焦关闭 |
| `src/components/Notes.tsx` | 空状态可点击创建笔记 |
| `src/App.tsx` | 首次访问弹窗延迟调整到 2s |

### 2026-06-03 第十八次迭代：手帐质感增强 + 天气动画 + 光斑背景

| 文件 | 改动 |
|------|------|
| `src/index.css` | 新增书脊阴影、装订孔、和纸胶带；卡片展开角装饰 |
| `src/components/Header.tsx` | 新增 `AnimatedWeatherIcon`（GSAP 晴天旋转/多云缓动/雨天跳动/雪天摆动/雾天闪烁） |
| `src/components/LightSpotBackground.tsx` | 新建：光斑漂移 + 波动渐变背景（亮色阳光/暗色月光），纯 CSS 零 JS 开销 |
| `src/App.tsx` | 集成 LightSpotBackground + 装订孔/胶带 HTML 元素；移除旧天气 Canvas |
| `src/components/BackgroundCanvas.tsx` | 已删除 |

### 2026-06-06 第十九次迭代：新增 6 个勋章 + 成就追踪

| 文件 | 改动 |
|------|------|
| `src/store/useDashboardStore.ts` | 新增 3 个持久化追踪字段：`earlyCheckinDates`、`lateNightNoteDates`、`viewedOldNoteDates`；修改 `toggleHabitDay`（早 8 点前标记早起）、`updateNote`（23 点后标记深夜）、`selectNote`（7 天前笔记标记回顾） |
| `src/components/AchievementsModal.tsx` | 新增 6 个勋章：晨间诗人、深夜抄经人、恰好路过、空页允许证、回头看一眼、手账小当家；各配手绘 SVG 图标 + 稀有度系统（normal/rare/epic/legendary） |

| 新勋章 | 条件 | 稀有度 |
|--------|------|--------|
| 晨间诗人 | 连续 5 天早 8 点前打卡 | 稀有 |
| 深夜抄经人 | 累计 10 次 23 点后记录 | 稀有 |
| 恰好路过 | 连续 3 天只完成 1 个待办 | 普通 |
| 空页允许证 | 有一天没有任何打卡 | 普通 |
| 回头看一眼 | 阅读 7 天前的笔记 | 普通 |
| 手账小当家 | 同时拥有任意 6 个勋章 | 传说 |

### 2026-06-06 第二十次迭代：勋章墙视觉重做

| 文件 | 改动 |
|------|------|
| `src/components/AchievementsModal.tsx` | 全面重写：双层边框 + 四角折角 + 波浪分割线 + 网格纸纹理背景 + Grid 2 列布局 + 稀有度彩色边框（普通无/稀有淡蓝/史诗淡紫/传说彩虹渐变）+ 稀有度标签 + 条件常驻显示 + 获得日期 + 偷看开关仅控寄语 + GSAP stagger 入场 + 新获得弹跳发光动画 + 滚动条圆角裁切 |

### 2026-06-06 第二十一次迭代：Steam 成就通知 + 首次引导 + 语音提示

| 文件 | 改动 |
|------|------|
| `src/components/AchievementNotify.tsx` | 新建：Steam 风格通知，右下角 280px 卡片，GSAP 弹性滑入 + 3s 停留 + 滑出，Web Audio C5→C6 和弦提示音，队列 FIFO 管理，稀有度光晕，`shownIds` 防重复 |
| `src/components/GuideTour.tsx` | 新建：首次访问引导遮罩，5 步骤介绍右上角按钮（成就/帮助/卡片/背景/主题），聚光灯镂空 + 脉冲边框 + 弹窗箭头指向，GSAP 入场动画，步骤指示器，ESC/跳过/下一步交互，localStorage 记录免重复 |
| `src/components/AchievementsModal.tsx` | `checkAchievements` 改用 `showAchievementNotify` 替代 `showToast`；三重守卫防重复触发 |
| `src/App.tsx` | 挂载 `AchievementNotify` + `GuideTour`；引导 1.8s 优先弹出，数据提示延迟 4s |

---

## 当前项目规模

- **源码文件**：38+ 组件 + 4 store/hooks + 2 utils
- **依赖**：React 18 / GSAP 3 / dnd-kit / Recharts / Zustand 5 / TailwindCSS 3 / lucide-react
- **构建产物**：~1000KB JS + 38KB CSS（gzip ~297KB + 8KB）
- **PWA**：manifest + Service Worker + 可安装
- **勋章总数**：10 个（4 原有 + 6 新增），含稀有度系统
